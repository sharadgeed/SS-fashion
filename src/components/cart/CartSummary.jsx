import { Link } from 'react-router-dom';
import { formatPrice } from '../../utils/formatPrice';
export default function CartSummary({ cart }) { const subtotal = cart.reduce((sum, item) => sum + item.price * item.qty, 0); const discount = cart.reduce((sum, item) => sum + (item.originalPrice - item.price) * item.qty, 0); const delivery = subtotal >= 2999 ? 0 : 149; return <aside className="cart-summary"><h2>Order summary</h2><div><span>Subtotal</span><b>{formatPrice(subtotal)}</b></div><div><span>Product discount</span><b className="green">−{formatPrice(discount)}</b></div><div><span>Delivery</span><b>{delivery ? formatPrice(delivery) : 'Free'}</b></div><div className="total"><span>Total</span><strong>{formatPrice(subtotal + delivery)}</strong></div><p>Inclusive of all taxes</p><Link to="/checkout" className="button">Proceed to checkout</Link></aside>; }
