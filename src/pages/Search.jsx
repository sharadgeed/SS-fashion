import { useLocation } from 'react-router-dom'; import ProductGrid from '../components/product/ProductGrid'; import EmptyState from '../components/common/EmptyState'; import { products } from '../data/products';
export default function Search(){const query=new URLSearchParams(useLocation().search).get('q')||'';const results=products.filter((product)=>(product.name+product.category+product.gender).toLowerCase().includes(query.toLowerCase()));return <main className="page"><div className="listing-head"><p className="eyebrow">SEARCH</p><h1>{query?`Results for “${query}”`:'Find your next favourite'}</h1><span>{results.length} results</span></div>{results.length?<ProductGrid products={results}/>:<EmptyState title="No pieces found." text="Try a different keyword, category, or collection." link="/new" label="Explore new arrivals"/>}</main>}
