import { products } from '../data/products'; import { useStore } from '../context/StoreContext'; import ProductGrid from '../components/product/ProductGrid'; import EmptyState from '../components/common/EmptyState';
export default function Wishlist(){const {wish}=useStore();const saved=products.filter((product)=>wish.includes(product.id));return <main className="page"><div className="listing-head"><p className="eyebrow">SAVED PIECES</p><h1>Wishlist</h1><span>{saved.length} pieces you love</span></div>{saved.length?<ProductGrid products={saved}/>:<EmptyState title="Your wishlist is empty." text="Save pieces you want to return to later." link="/new" label="Explore collection"/>}</main>}
